\* Both halves present: resolve treats a tombstone tail as deleted AND the tombstone
\* is an in-order seq+1 CAS append. Expected: PASS — resolve always matches the single
\* append-only order, non-vacuously (delete, advance, and delete-then-recreate reachable).
SPECIFICATION Spec
CONSTANTS
    SkipTombstonedTail = TRUE
    TombstoneIsCasEvent = TRUE
INVARIANTS
    TypeOk
    Safe
