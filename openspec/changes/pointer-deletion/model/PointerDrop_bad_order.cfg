\* Tombstone written out-of-band, NOT through the seq+1 CAS append. Expected: FAIL —
\* the delete is outside the append-only order, so it collides incoherently with a
\* concurrent advance (a legitimately-advanced pointer reads as deleted, or vice versa).
SPECIFICATION Spec
CONSTANTS
    SkipTombstonedTail = TRUE
    TombstoneIsCasEvent = FALSE
INVARIANTS
    TypeOk
    Safe
