\* Reader does NOT treat a tombstone tail as deleted. Expected: FAIL — a deleted
\* pointer (tombstone tail) resolves to the stale value below the tombstone.
SPECIFICATION Spec
CONSTANTS
    SkipTombstonedTail = FALSE
    TombstoneIsCasEvent = TRUE
INVARIANTS
    TypeOk
    Safe
