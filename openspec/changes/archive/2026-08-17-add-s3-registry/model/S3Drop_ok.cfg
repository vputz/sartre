\* Both halves present: resolve skips tombstoned AND tombstone precedes delete.
\* Expected: PASS — no reader ever resolves to a reclaimed manifest, AND the spec is
\* non-vacuous (a promote can succeed and GC can still reclaim v).
SPECIFICATION Spec
CONSTANTS
    SkipTombstoned = TRUE
    TombstoneFirst = TRUE
INVARIANTS
    TypeOk
    Safe
