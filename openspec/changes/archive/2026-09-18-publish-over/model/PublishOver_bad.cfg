\* CAS expected := freshly-read head. Expected: FAIL — a publish that moves head
\* between the base read and the advance is silently overwritten by stale-based content.
SPECIFICATION Spec
CONSTANTS
    UseBaseExpected = FALSE
    MaxVer = 4
INVARIANTS
    TypeOk
    Safe
