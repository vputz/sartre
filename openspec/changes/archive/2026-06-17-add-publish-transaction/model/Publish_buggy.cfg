\* Wrong ordering: pointer advanced before manifest commit / blob storage.
\* Expected: CHECK_FAILED on PointerSafe (and LogConsistent) — the tip
\* references an uncommitted manifest in the window before Commit.
SPECIFICATION Spec

CONSTANTS
    Publishers = {p1, p2}
    NoTip = NoTip
    PointerFirst = TRUE

INVARIANTS
    TypeOk
    PointerSafe
    LogConsistent
